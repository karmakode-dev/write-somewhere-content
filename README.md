# Assets

Static files and manifests for deployment.

`licenses.json` is the public official-content license list. Write Somewhere `/licenses` fetches it from this repo after publish. Personal/local user files are never listed here.

See internal notes for maintenance.
